var answer = window.prompt('Help?');
if (answer === 'yes') {
  alert('Tap to jump, Avoid obstacles');
} else if (answer === 'no') {
  window.alert('loading...');
} else {
  window.alert('Answer "yes" or "no"');
}
